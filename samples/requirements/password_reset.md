# Password Reset Flow

Build a secure password reset experience for a web application.

Users should be able to request a reset link by email, open a time-limited link, set a new password, and see clear confirmation when the password has changed. The flow must handle expired links, invalid tokens, reused passwords, and inaccessible email addresses.

The implementation should include export-friendly ticket descriptions and acceptance criteria.

